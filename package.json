{
  "name": "gql-error",
  "version": "0.4.0",
  "description": "Used to throw graphql error during validation",
  "main": "./lib",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf lib && mkdir lib && babel src --presets babel-preset-es2015 --out-dir lib",
    "prepublish": "npm run build"
  },
  "keywords": [
    "graphql",
    "validator",
    "error"
  ],
  "author": "Raj Krishna Maharjan",
  "license": "ISC",
  "dependencies": {
    "graphql": "^0.11.7",
    "lodash": "^4.17.4",
    "validate.js": "^0.11.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
